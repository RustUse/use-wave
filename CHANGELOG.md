# Changelog

## Unreleased

### Added

- Added the initial `use-wave` v0.1 crate with validated wave relationships,
  period, angular frequency, wave number, and phase helpers.
- Added README-driven crate docs, baseline CI, a manual publish workflow, and
  `release-plz` configuration for follow-up releases.
